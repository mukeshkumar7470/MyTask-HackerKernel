<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mukeshkpdeveloper.mytask">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_my_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_my_logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyTask">
        <activity android:name=".UI.activity.FullScreenImageActivity"></activity>
        <activity
            android:name=".UI.activity.LoginActivity"
            android:theme="@style/Theme.MyTask"
            android:windowSoftInputMode="stateVisible" />
        <activity android:name=".UI.activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UI.activity.MainActivity" />
    </application>

</manifest>